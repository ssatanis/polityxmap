{
  "name": "polityxmap",
  "version": "1.0.0",
  "description": "PolityxMap Web Application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "deploy": "git init && git add . && git checkout -B main && git commit -m 'Initial commit' && git push -f origin main"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.3",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-session": "^1.17.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.6",
    "nodemon": "^3.1.10"
  }
}
